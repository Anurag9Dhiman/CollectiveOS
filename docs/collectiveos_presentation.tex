%% CollectiveOS — Beamer Presentation
%% Structure: Sequoia arc (Purpose → Problem → Solution → Why Now → Product → Proof → Audiences → Brief)
%% Theme: Metropolis (light background); accentcol (#1E293B) used ONLY on bars
%% Compile: pdflatex collectiveos_presentation.tex  (twice for TOC/refs)

\documentclass[aspectratio=169, 10pt]{beamer}

% ── Theme ──────────────────────────────────────────────────────────────────
\usetheme[progressbar=frametitle, block=fill, sectionpage=progressbar]{metropolis}
\usepackage{appendixnumberbeamer}

% ── Fonts ──────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}

% ── Colour Palette ─────────────────────────────────────────────────────────
\definecolor{accentcol}{HTML}{1E293B}   % bars only (#1E293B as specified)
\definecolor{skyblue}  {HTML}{2563EB}   % primary highlight
\definecolor{greencol} {HTML}{16A34A}   % live / success
\definecolor{ambercol} {HTML}{D97706}   % caution / HITL
\definecolor{redcol}   {HTML}{DC2626}   % problem / warning
\definecolor{indigocol}{HTML}{6D28D9}   % technical / secondary
\definecolor{dimcol}   {HTML}{64748B}   % muted text
\definecolor{lightbg}  {HTML}{F8FAFC}   % slide background
\definecolor{cardbg}   {HTML}{EFF6FF}   % card / row tint

% ── Apply colours ──────────────────────────────────────────────────────────
\setbeamercolor{normal text}          {fg=accentcol, bg=lightbg}
\setbeamercolor{alerted text}         {fg=skyblue}
\setbeamercolor{example text}         {fg=greencol}
\setbeamercolor{frametitle}           {fg=white,  bg=accentcol}   % bar ← accentcol
\setbeamercolor{progress bar}         {fg=skyblue,bg=accentcol!40}% bar ← accentcol
\setbeamercolor{title separator}      {fg=accentcol}              % bar ← accentcol
\setbeamercolor{section in head/foot} {fg=white,  bg=accentcol}   % bar ← accentcol
\setbeamercolor{block title}          {fg=white,  bg=skyblue}
\setbeamercolor{block body}           {fg=accentcol, bg=cardbg}
\setbeamercolor{block title alerted}  {fg=white,  bg=ambercol}
\setbeamercolor{block body alerted}   {fg=accentcol, bg=cardbg}
\setbeamercolor{block title example}  {fg=white,  bg=greencol}
\setbeamercolor{block body example}   {fg=accentcol, bg=cardbg}
\setbeamercolor{palette primary}      {fg=white,  bg=accentcol}
\setbeamercolor{item}                 {fg=skyblue}
\setbeamercolor{subitem}              {fg=indigocol}

% ── Packages ───────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{colortbl}          % \rowcolor, \columncolor
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, fit, calc, decorations.pathreplacing}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{fontawesome5}
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

% ── tcolorbox card styles ───────────────────────────────────────────────────
\tcbset{
  card/.style={
    enhanced, colback=white, colframe=accentcol!15,
    arc=5pt, boxrule=0.7pt, left=7pt, right=7pt, top=5pt, bottom=5pt
  },
  % Titled card: coloured stripe on the left + title badge
  skycard/.style  ={card, colframe=skyblue!35,
    borderline west={3pt}{0pt}{skyblue},
    fonttitle=\small\bfseries\color{skyblue}},
  greencard/.style={card, colframe=greencol!35,
    borderline west={3pt}{0pt}{greencol},
    fonttitle=\small\bfseries\color{greencol}},
  ambercard/.style={card, colframe=ambercol!35,
    borderline west={3pt}{0pt}{ambercol},
    fonttitle=\small\bfseries\color{ambercol}},
  indigocard/.style={card,colframe=indigocol!35,
    borderline west={3pt}{0pt}{indigocol},
    fonttitle=\small\bfseries\color{indigocol}},
  redcard/.style  ={card, colframe=redcol!35,
    borderline west={3pt}{0pt}{redcol},
    fonttitle=\small\bfseries\color{redcol}},
}

% ── Helper macros ──────────────────────────────────────────────────────────
\newcommand{\hl}[1]{\textcolor{skyblue}{\textbf{#1}}}
\newcommand{\live}[1]{\textcolor{greencol}{\small\faCircle}\;\textcolor{accentcol}{#1}}

% Chip: tiny coloured pill for component tags inside TikZ
\newcommand{\chip}[2]{%
  {\setlength{\fboxsep}{2pt}\colorbox{#1!15}{\textcolor{#1}{\tiny\bfseries\,#2\,}}}%
}

% ── Meta ───────────────────────────────────────────────────────────────────
\title{\textbf{CollectiveOS}}
\subtitle{Personal AI Operating System}
\author{Anurag Dhiman}
\date{2026}
\institute{}

% ═══════════════════════════════════════════════════════════════════════════
\begin{document}

% ── 01. Title ──────────────────────────────────────────────────────────────
{
\setbeamercolor{background canvas}{bg=accentcol}
\begin{frame}[plain]
  \begin{columns}[c]
    \column{0.44\textwidth}
      \vspace{1em}
      {\fontsize{30}{34}\selectfont\bfseries\color{white} CollectiveOS}\\[6pt]
      {\large\color{white!60} Personal AI Operating System}\\[20pt]
      {\small\color{white!40} Anurag Dhiman · 2026}

      \vspace{1.4em}
      \begin{tikzpicture}[scale=0.7]
        \foreach \col/\lbl in {skyblue/85+\,Tools, greencol/3\,Agents, ambercol/\infty\,Memory}{
          \draw[\col!60, line width=1pt] (0,0)--(0.8,0);
          \node[right, font=\scriptsize\bfseries, text=\col] at (0.85,0) {\lbl};
          \coordinate (last) at (0, -0.55);
          \begin{scope}[yshift=-0.55cm]
          \end{scope}
        }
      \end{tikzpicture}
      % simple bullet stats
      \par\vspace{-0.3em}
      {\scriptsize\color{skyblue}\textbf{85+} Tools\quad}
      {\scriptsize\color{greencol}\textbf{3} Peer Agents\quad}
      {\scriptsize\color{ambercol}\textbf{$\infty$} Memory\quad}
      {\scriptsize\color{white!50}\textbf{0} Cloud Data}

    \column{0.52\textwidth}
      \centering
      \begin{tikzpicture}[
        hub/.style={circle, minimum size=22mm, font=\scriptsize\bfseries},
        spoke/.style={circle, minimum size=12mm, font=\fontsize{5}{6}\selectfont\bfseries,
                      align=center, inner sep=1pt},
        arr/.style={->, >=Stealth, line width=0.7pt},
      ]
        % Hub
        \node[hub, fill=skyblue!25, draw=skyblue!60, text=white] (H) at (0,0)
          {\color{white}\faLayerGroup\\[2pt]\color{white!80}\tiny Orchestrator};

        % Spokes with icons, evenly around the circle
        \foreach \lbl/\icon/\col/\ang/\r in {
          VoiceOS/\faMicrophone/skyblue/90/2.2,
          VisualOS/\faCamera/indigocol/35/2.1,
          Gmail/\faEnvelope/redcol/340/2.1,
          Calendar/\faCalendar/skyblue/295/2.2,
          Health/\faHeartbeat/greencol/250/2.1,
          Home/\faHome/ambercol/205/2.1,
          Slack/\faSlack/indigocol/155/2.2
        }{
          \node[spoke, fill=\col!15, draw=\col!40, text=\col!80] (\lbl) at (\ang:\r)
            {\icon\\[1pt]\lbl};
          \draw[arr, \col!40] (H)--(\lbl);
        }
        % Outer connector label
        \node[font=\fontsize{5}{6}\selectfont\color{white!40}] at (0,-2.6)
          {+ 80 more connectors};
      \end{tikzpicture}
  \end{columns}
\end{frame}
}

% ── 02. Agenda ─────────────────────────────────────────────────────────────
\begin{frame}{Agenda}
  \setbeamertemplate{section in toc}[sections numbered]
  \tableofcontents[hideallsubsections]
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{The Problem}
% ═══════════════════════════════════════════════════════════════════════════

% ── 03. Hook ───────────────────────────────────────────────────────────────
\begin{frame}{You open 20 apps every morning. None of them know each other.}
  \begin{columns}[c]
    \column{0.46\textwidth}
      {\large\bfseries\color{accentcol}
        Gmail doesn't know your calendar.\\[5pt]
        Slack doesn't know your tasks.\\[5pt]
        Your AI forgets you the moment\\
        you close it.}

      \vspace{1em}
      {\color{dimcol}\small
        You are the only thing connecting all of it.\\
        Every morning. Every day. Manually.}

    \column{0.50\textwidth}
      \centering
      % Chaos diagram: scattered apps, broken connections to "You"
      \begin{tikzpicture}[
        scale=0.82, transform shape,
        app/.style={draw=dimcol!35, fill=white, rounded corners=3pt,
                    minimum width=1.75cm, minimum height=0.5cm,
                    font=\fontsize{6}{7}\selectfont, align=center, inner sep=2pt},
        arr/.style={dimcol!25, dashed, thick, shorten >=4pt, shorten <=4pt},
      ]
        % Central "You" — the manual connector
        \node[circle, draw=redcol, fill=redcol!10, text=redcol,
              minimum size=1.3cm, font=\small\bfseries] (Y) at (0,0) {You};

        % Apps scattered around (angle : distance)
        \node[app] (G)  at (82:2.6)    {\faEnvelope\;Gmail};
        \node[app] (Ca) at (37:2.5)    {\faCalendar\;Calendar};
        \node[app] (Sl) at (0:2.7)     {\faSlack\;Slack};
        \node[app] (No) at (330:2.5)   {\faStickyNote\;Notion};
        \node[app] (To) at (290:2.6)   {\faTasks\;Todoist};
        \node[app] (He) at (245:2.5)   {\faHeartbeat\;Health};
        \node[app] (Sp) at (205:2.6)   {\faMusic\;Spotify};
        \node[app] (Te) at (165:2.5)   {\faTelegram\;Telegram};
        \node[app] (Gi) at (128:2.7)   {\faGithub\;GitHub};

        % Broken dotted spokes
        \foreach \n in {G,Ca,Sl,No,To,He,Sp,Te,Gi}
          \draw[arr] (Y)--(\n);

        % "×" break markers on four spokes to show disconnection
        \foreach \pt in {
          {(0.5, 1.0)}, {(1.1, 0.3)}, {(0.1,-1.1)}, {(-0.9, 0.6)}
        }
          \node[text=redcol, font=\bfseries\scriptsize] at \pt {$\times$};

        % No connection between apps at all — this is the point
        \node[font=\fontsize{5}{6}\selectfont\color{dimcol}, text width=1.3cm, align=center]
          at (0, -2.9) {zero app-to-app\\connections};
      \end{tikzpicture}
  \end{columns}
\end{frame}

% ── 04. Problem ────────────────────────────────────────────────────────────
\begin{frame}{Digital fragmentation costs hours of productivity every week}
  \begin{columns}[T]
    \column{0.50\textwidth}
      \begin{tcolorbox}[redcard, title={\faExclamationTriangle\; The Fragmented Reality}]
        \small
        \begin{itemize}\setlength\itemsep{3pt}
          \item Average person juggles \hl{15–20 apps} daily
          \item Every app is a \textbf{silo} — nothing shares context
          \item AI chatbots have \textbf{zero memory} across sessions
          \item Automation tools require coding knowledge
          \item Context-switching drains focus and time
        \end{itemize}
      \end{tcolorbox}
      \vspace{5pt}
      \begin{tcolorbox}[card]
        \small\color{dimcol}\itshape
        ``The problem isn't the apps. It's the absence of a layer that connects them and remembers everything.''
      \end{tcolorbox}

    \column{0.46\textwidth}
      \centering\vspace{0.5em}

      % Big stats with visual weight
      \begin{tikzpicture}[scale=0.9]
        % Grid of 4 stats
        \foreach \val/\desc/\col/\x/\y in {
          {20+}/{context switches\\per day}/redcol/0/1.4,
          {0}/{apps that\\remember you}/redcol/3.6/1.4,
          {100\%}/{manual effort —\\you are the glue}/redcol/0/0,
          {\$\$\$}/{per-month\\subscription cost}/ambercol/3.6/0
        }{
          \node[align=center] at (\x, \y)
            {{\fontsize{22}{24}\selectfont\bfseries\color{\col}\val}\\[3pt]
             {\fontsize{7}{9}\selectfont\color{dimcol}\desc}};
        }
      \end{tikzpicture}
  \end{columns}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{The Solution}
% ═══════════════════════════════════════════════════════════════════════════

% ── 05. Purpose ────────────────────────────────────────────────────────────
\begin{frame}{CollectiveOS: one agent that knows your life and acts across all of it}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \begin{tcolorbox}[greencard, title={\faCheckCircle\; In One Sentence}]
        \normalsize
        A \hl{personal AI operating system} that connects every digital service you use, remembers every conversation, understands your context, and acts on your behalf — with your permission, on your hardware.
      \end{tcolorbox}
      \vspace{5pt}
      \begin{tcolorbox}[skycard, title={\faExchangeAlt\; Before vs.\ After}]
        \small
        \textbf{Before:}\enspace 20 apps, manual switching, zero memory\\[4pt]
        \textbf{After:}\enspace One sentence. It handles the rest.
      \end{tcolorbox}

    \column{0.48\textwidth}
      \centering\vspace{0.3em}
      \begin{tikzpicture}[scale=0.9]
        % Four pillars
        \foreach \icon/\val/\desc/\col/\x in {
          \faWrench/{85+}/{Live\\Tools}/skyblue/0,
          \faRobot/{3}/{Peer\\Agents}/indigocol/2.2,
          \faBrain/{$\infty$}/{Persistent\\Memory}/greencol/4.4,
          \faCloud/{0}/{Cloud Data\\Sent}/ambercol/6.6
        }{
          \fill[\col!10] (\x-0.9,-0.4) rectangle (\x+0.9,2.4);
          \draw[\col!40, rounded corners=3pt] (\x-0.9,-0.4) rectangle (\x+0.9,2.4);
          \node[\col, font=\scriptsize] at (\x, 2.0) {\icon};
          \node[font=\fontsize{18}{20}\selectfont\bfseries, text=\col] at (\x, 1.2) {\val};
          \node[font=\fontsize{7}{9}\selectfont, text=dimcol, align=center, text width=1.6cm] at (\x, 0.2) {\desc};
        }
      \end{tikzpicture}

      \vspace{6pt}
      \begin{tcolorbox}[ambercard, title={\faShieldAlt\; Safety by Architecture}]
        \small
        Reads: \textbf{instant}\enspace·\enspace
        Writes: \textbf{always confirms first}\enspace·\enspace
        Data: \textbf{stays on your machine}
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ── 06. Why Now ────────────────────────────────────────────────────────────
\begin{frame}{Three forces converging make 2026 the right moment}
  \begin{columns}[T]
    \column{0.62\textwidth}
      \begin{tcolorbox}[skycard, title={\faBolt\; 01 · AI Is Finally Capable Enough}]
        \small
        Gemini Flash can reason across long tool-calling chains and hold context reliably. Multi-step orchestration that failed two years ago is now table-stakes.
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[indigocard, title={\faLock\; 02 · Privacy Concerns Are Peaking}]
        \small
        Users refuse to hand personal history to cloud AI. Local-first isn't just technically possible — it's what users are demanding. CollectiveOS is local \emph{by architecture}, not policy.
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[greencard, title={\faMicrophone\; 03 · Voice Is the Next Platform}]
        \small
        Gemini Live enables full-duplex audio AI. The shift from screen-first to voice-first is happening now. CollectiveOS already supports ``Hey Collective'' wake-word voice.
      \end{tcolorbox}

    \column{0.34\textwidth}
      \centering\vspace{0.5em}
      % Convergence diagram
      \begin{tikzpicture}[scale=0.9, transform shape,
        arr/.style={->, >=Stealth, line width=2pt, shorten >=3pt}
      ]
        % Three labelled arrows converging on "NOW"
        \node[circle, fill=accentcol, text=white, minimum size=1.4cm,
              font=\small\bfseries] (N) at (0,0) {NOW};

        \draw[arr, skyblue]   (-2.8, 1.4) node[left, font=\tiny\bfseries\color{skyblue}, align=right, text width=1.7cm]
          {AI\\Capability} -- (N);

        \draw[arr, indigocol] (-2.8, 0.0) node[left, font=\tiny\bfseries\color{indigocol}, align=right, text width=1.7cm]
          {Privacy\\Demand} -- (N);

        \draw[arr, greencol]  (-2.8,-1.4) node[left, font=\tiny\bfseries\color{greencol}, align=right, text width=1.7cm]
          {Voice\\Platform} -- (N);

        % No consumer product fills the gap
        \node[font=\fontsize{6}{8}\selectfont\color{dimcol}, align=center, text width=2.5cm] at (0,-1.4)
          {no consumer product is\\stateful + integrated + private};
      \end{tikzpicture}

      \vspace{6pt}
      \begin{tcolorbox}[card]
        \centering\scriptsize\color{dimcol}
        \textbf{CollectiveOS is all three.}
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{How It Works}
% ═══════════════════════════════════════════════════════════════════════════

% ── 07. Architecture ───────────────────────────────────────────────────────
\begin{frame}{Five layers, each with one job — nothing reaches past its neighbour}
  \centering
  \begin{tikzpicture}[scale=0.84, transform shape]

    % ── Layer macro: \lyr{y}{fill color}{icon}{L#}{Name}{components}
    \foreach \y/\fc/\icon/\ln/\nm/\comps in {
      4.20/skyblue/\faUser/L4/{You (Input)}/{%
        \chip{skyblue}{Voice}\enspace
        \chip{skyblue}{Chat UI}\enspace
        \chip{skyblue}{iOS /ask}\enspace
        \chip{skyblue}{REST /chat}\enspace
        \chip{skyblue}{Telegram}},
      3.05/ambercol/\faSitemap/L3/{Orchestrator}/{%
        \chip{ambercol}{Intent Router}\enspace
        \chip{ambercol}{Visual Routing}\enspace
        \chip{ambercol}{Context Enrich}\enspace
        \chip{ambercol}{Agent Registry}\enspace
        \chip{ambercol}{HITL Gate}},
      1.90/indigocol/\faRobot/L2/{Peer Agents}/{%
        \chip{indigocol}{Task Agent (LangGraph)}\enspace
        \chip{indigocol}{VisualOS — Screen AI}\enspace
        \chip{indigocol}{VoiceOS — Gemini Live}},
      0.75/greencol/\faWrench/L1/{Tool Layer}/{%
        \chip{greencol}{85+ Connectors}\enspace
        \chip{greencol}{MCP Servers}\enspace
        \chip{greencol}{HITL Write Gate}\enspace
        \chip{greencol}{Output Bus (Push·Slack·TG)}},
      -0.40/accentcol/\faDatabase/L0/{Memory \& Data}/{%
        \chip{accentcol}{PostgreSQL}\enspace
        \chip{accentcol}{pgvector HNSW}\enspace
        \chip{accentcol}{Knowledge Graph}\enspace
        \chip{accentcol}{LangGraph Checkpoints}\enspace
        \chip{accentcol}{Redis}}
    }{
      % Badge cell (left, solid colour, 1.8 cm wide)
      \fill[\fc] (-7.2, \y-0.42) rectangle (-5.3, \y+0.42);
      \node[text=white, font=\fontsize{7}{8}\selectfont\bfseries,
            align=center, text width=1.7cm] at (-6.25, \y)
        {\icon\\[2pt]\ln};

      % Body cell (right, light tint)
      \fill[\fc!10] (-5.3, \y-0.42) rectangle (7.2, \y+0.42);
      \draw[\fc!30] (-7.2, \y-0.42) rectangle (7.2, \y+0.42);

      \node[text=accentcol, font=\scriptsize, align=left,
            anchor=west, text width=11.8cm, inner sep=0pt] at (-5.1, \y+0.1)
        {\textbf{\nm}};
      \node[text=accentcol!70, font=\fontsize{7}{8}\selectfont, align=left,
            anchor=west, text width=11.8cm, inner sep=0pt] at (-5.1, \y-0.18)
        {\comps};
    }

    % Arrows between layers
    \foreach \ya/\yb/\col/\lbl in {
      3.78/3.47/skyblue/{HTTP · WS · JSON},
      2.63/2.32/ambercol/{capability dispatch},
      1.48/1.17/indigocol/{tool calls · REST · MCP},
      0.33/0.02/greencol/{PostgreSQL · Redis}
    }{
      \draw[->, >=Stealth, \col!70, thick] (0, \ya)--(0, \yb);
      \node[font=\fontsize{6}{7}\selectfont\color{dimcol}, right] at (0.12, {(\ya+\yb)/2}) {\lbl};
    }
  \end{tikzpicture}
\end{frame}

% ── 08. Agent Loop ─────────────────────────────────────────────────────────
\begin{frame}{Every message travels the same loop — and always saves what it learned}
  \centering
  \begin{tikzpicture}[
    scale=0.85, transform shape,
    step/.style={draw, rounded corners=5pt,
                 minimum width=2.5cm, minimum height=1.25cm,
                 font=\fontsize{7}{8}\selectfont\bfseries,
                 align=center, text width=2.3cm, inner sep=3pt},
    arr/.style={->, >=Stealth, thick},
    ann/.style={font=\fontsize{6}{7}\selectfont\color{dimcol},
                align=center, text width=2.5cm},
  ]
    % ── TOP ROW: steps 1-4 (left → right, y = 1.8) ────────────────────────
    \node[step, fill=skyblue!12,   draw=skyblue!55,   text=skyblue]    (s1) at (0.0, 1.8)
      {\faCommentDots\\[3pt]01\\Message In};
    \node[step, fill=ambercol!12,  draw=ambercol!55,  text=ambercol]   (s2) at (3.0, 1.8)
      {\faSitemap\\[3pt]02\\Route Intent};
    \node[step, fill=skyblue!12,   draw=skyblue!55,   text=skyblue]    (s3) at (6.0, 1.8)
      {\faBrain\\[3pt]03\\Memory Inject};
    \node[step, fill=indigocol!12, draw=indigocol!55, text=indigocol]  (s4) at (9.0, 1.8)
      {\faRobot\\[3pt]04\\Gemini Reasons};

    % ── BOTTOM ROW: steps 5-8 (right → left, y = 0) ───────────────────────
    \node[step, fill=greencol!12,  draw=greencol!55,  text=greencol]   (s5) at (9.0, 0.0)
      {\faWrench\\[3pt]05\\Tool Execute};
    \node[step, fill=redcol!10,    draw=redcol!40,    text=redcol]     (s6) at (6.0, 0.0)
      {\faUserCheck\\[3pt]06\\HITL Gate};
    \node[step, fill=indigocol!12, draw=indigocol!55, text=indigocol]  (s7) at (3.0, 0.0)
      {\faReply\\[3pt]07\\Loop / Reply};
    \node[step, fill=greencol!12,  draw=greencol!55,  text=greencol]   (s8) at (0.0, 0.0)
      {\faSave\\[3pt]08\\Save \& Deliver};

    % ── Flow arrows ────────────────────────────────────────────────────────
    \draw[arr, skyblue!60]   (s1)--(s2);
    \draw[arr, skyblue!60]   (s2)--(s3);
    \draw[arr, skyblue!60]   (s3)--(s4);
    \draw[arr, greencol!70]  (s4)--(s5);   % right-side bend
    \draw[arr, greencol!60]  (s5)--(s6);
    \draw[arr, dimcol!50]    (s6)--(s7);
    \draw[arr, dimcol!50]    (s7)--(s8);

    % ── Loop-back: 07 → 04, arc over the top ──────────────────────────────
    \draw[->, >=Stealth, dashed, thick, indigocol!55, bend left=30]
      (s7.north) to
      node[above, font=\fontsize{6}{7}\selectfont\color{indigocol},
           text width=2.2cm, align=center] {more tools\\needed?}
      (s4.south);

    % ── Annotations outside the U ──────────────────────────────────────────
    % Above top row
    \node[ann, above=5pt of s1] {Voice · Chat\\API input};
    \node[ann, above=5pt of s2] {Visual? → VisualOS\\Scan? → Enrich};
    \node[ann, above=5pt of s3] {RAG + Graph\\in system prompt};
    \node[ann, above=5pt of s4] {Reply direct\\or call a tool};

    % Below bottom row
    \node[ann, below=5pt of s8] {Memory saved\\Push / Voice out};
    \node[ann, below=5pt of s7] {Done when no\\tool is left};
    \node[ann, below=5pt of s6] {``Send this?''\\Yes / No};
    \node[ann, below=5pt of s5] {Read: instant\\Write: paused};

  \end{tikzpicture}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{Live Today}
% ═══════════════════════════════════════════════════════════════════════════

% ── 09. Connectors ─────────────────────────────────────────────────────────
\begin{frame}{85+ connectors — every service in your life is reachable right now}
  \begin{columns}[T]
    \column{0.50\textwidth}
      \begin{tcolorbox}[skycard, title={\faCalendar\; Productivity \& Workspace}]
        \scriptsize
        \live{Google Calendar}\quad\live{Gmail}\quad\live{Google Drive}\\[2pt]
        \live{Todoist}\quad\live{Notion}\quad\live{GitHub}
      \end{tcolorbox}
      \vspace{3pt}
      \begin{tcolorbox}[indigocard, title={\faComments\; Communication}]
        \scriptsize
        \live{iMessage}\quad\live{Slack}\quad\live{Telegram}\quad\live{iOS Push}
      \end{tcolorbox}
      \vspace{3pt}
      \begin{tcolorbox}[card, title={\textbf{\faApple\; Mac \& System}}, colframe=accentcol!20, borderline west={3pt}{0pt}{accentcol}]
        \scriptsize
        \live{Screen Capture}\quad\live{Browser Control}\quad\live{Local Files}\\[2pt]
        \live{Clipboard}\quad\live{Contacts}\quad\live{Notes}\quad\live{Reminders}\\[2pt]
        \live{Computer Use Agent}
      \end{tcolorbox}

    \column{0.46\textwidth}
      \begin{tcolorbox}[greencard, title={\faHome\; Smart Home \& Devices}]
        \scriptsize
        \live{Home Assistant (lights, switches)}\\[2pt]
        \live{Appliances (status \& control)}\quad\live{Car Status}
      \end{tcolorbox}
      \vspace{3pt}
      \begin{tcolorbox}[ambercard, title={\faHeartbeat\; Health, Finance \& Media}]
        \scriptsize
        \live{Apple Health (sleep, HRV, activity)}\\[2pt]
        \live{Finance (accounts, transactions)}\\[2pt]
        \live{Spotify}\quad\live{Wearable (Frame / Garmin)}
      \end{tcolorbox}
      \vspace{3pt}
      \begin{tcolorbox}[indigocard, title={\faRobot\; AI, Vision \& Robotics}]
        \scriptsize
        \live{VisualOS (screen \& image AI)}\quad\live{Web Search}\\[2pt]
        \live{AI Ask / Compare}\quad\live{ROS2 Robot}\\[2pt]
        \live{MCP Servers}\quad\live{Condition Watchers}
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{For Every Audience}
% ═══════════════════════════════════════════════════════════════════════════

% ── 10. General User ───────────────────────────────────────────────────────
\begin{frame}{For everyday users — your morning routine in one sentence}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \begin{tcolorbox}[skycard, title={\faSun\; Morning Briefing (Voice)}]
        \small
        \textit{``Good morning — what's my day?''}\\[3pt]
        {\color{dimcol}Reads calendar + Gmail + health sleep score.
        Gives a 30-second spoken summary. One question — everything covered.}
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[greencard, title={\faCheckCircle\; Action With Confirmation}]
        \small
        \textit{``Add a 3pm meeting with Sarah and email the invite.''}\\[3pt]
        {\color{dimcol}Creates event, drafts email, shows you exactly what it'll do, asks \textbf{``Send this?''} — only executes on your yes.}
      \end{tcolorbox}

    \column{0.48\textwidth}
      \begin{tcolorbox}[ambercard, title={\faBrain\; Memory Across Sessions}]
        \small
        \textit{``What did I discuss with Sarah last week?''}\\[3pt]
        {\color{dimcol}Searches semantic memory across all past conversations. No re-explaining context. No inbox archaeology.}
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[indigocard, title={\faMicrophone\; Voice-First}]
        \small
        Say \hl{``Hey Collective''} → then just talk.\\[3pt]
        {\color{dimcol}Full-duplex Gemini Live audio. Real-time, natural, speaks back. Works while your hands are busy.}
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ── 11. Real Scenarios ─────────────────────────────────────────────────────
\begin{frame}{Six things you can do right now — no setup, no coding}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \small
      \textcolor{skyblue}{\textbf{\faHome\; Smart Home}}\\
      {\color{dimcol}\textit{``Turn living room to warm white and play lo-fi.''}\\
      Both execute instantly — low-risk actions need no confirmation.}

      \vspace{0.7em}
      \textcolor{greencol}{\textbf{\faHeartbeat\; Health Insight}}\\
      {\color{dimcol}\textit{``How did I sleep this week? Is my HRV trending up?''}\\
      Pulls Apple Health, interprets trends, plain-English summary.}

      \vspace{0.7em}
      \textcolor{indigocol}{\textbf{\faCamera\; Screen Vision}}\\
      {\color{dimcol}\textit{``What's on my screen right now?''}\\
      Screenshot → VisualOS → Structured card in $\sim$1 second. Session saved for follow-up.}

    \column{0.48\textwidth}
      \small
      \textcolor{ambercol}{\textbf{\faSlack\; Work Messaging}}\\
      {\color{dimcol}\textit{``Slack Priya that I'll be 10 minutes late.''}\\
      Drafts, shows you, confirms, sends.}

      \vspace{0.7em}
      \textcolor{skyblue}{\textbf{\faBell\; Proactive Watcher}}\\
      {\color{dimcol}\textit{``Alert me when Alice emails.''}\\
      Condition Watcher polls Gmail. Notifies via push when triggered.}

      \vspace{0.7em}
      \textcolor{greencol}{\textbf{\faChartBar\; Finance}}\\
      {\color{dimcol}\textit{``How much did I spend on food last month?''}\\
      Queries transactions, aggregates by category, responds.}
  \end{columns}
\end{frame}

% ── 12. Decision-Makers ────────────────────────────────────────────────────
\begin{frame}{For decision-makers — the strategic view across six dimensions}
  \small
  \setlength{\tabcolsep}{8pt}
  \renewcommand{\arraystretch}{1.35}
  \rowcolors{2}{cardbg}{white}
  \begin{tabularx}{\textwidth}{>{\bfseries\color{skyblue}\small}p{3.0cm} X}
    \arrayrulecolor{accentcol!20}
    \toprule
    {\normalfont\color{dimcol}\texttt{Dimension}} &
    {\normalfont\color{dimcol}\texttt{Answer}} \\
    \midrule
    Market Gap &
      General AI assistants are stateless and siloed. CollectiveOS is \hl{stateful, integrated, and private} — the gap no consumer product fills. \\
    Differentiation &
      Runs on your hardware. Data never leaves your machine. No per-service subscription. One agent, full capability. \\
    AI Strategy &
      Model-agnostic by design. Currently Gemini Flash — swappable. Intelligence is in the \hl{orchestration layer}, not the model. \\
    Scalability &
      Hub-and-spoke: every new connector adds capability without touching the core. Plug in any API, MCP server, or peer agent. \\
    Risk Profile &
      All write actions require \hl{human confirmation (HITL)}. No autonomous sending, no unsupervised device control. Safety-first by architecture. \\
    Roadmap &
      Phased build: read-only → write → memory → voice → vision → robotics. Each phase proven before the next begins. \\
    \bottomrule
  \end{tabularx}
\end{frame}

% ── 13. Technical ──────────────────────────────────────────────────────────
\begin{frame}{For engineers — the full stack, every component chosen with purpose}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \begin{tcolorbox}[skycard, title={\faCode\; Agent Framework}]
        \scriptsize\ttfamily\setlength\itemsep{1pt}
        \begin{tabular}{@{}ll@{}}
          Framework:   & LangGraph StateGraph \\
          Checkpoint:  & PostgresSaver \\
          HITL:        & interrupt\_before [write\_tools] \\
          LLM API:     & Gemini function-calling \\
          Concurrency: & ThreadPoolExecutor \\
        \end{tabular}
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[indigocard, title={\faDatabase\; Memory System}]
        \scriptsize\ttfamily
        \begin{tabular}{@{}ll@{}}
          DB:         & PostgreSQL + pgvector \\
          Index:      & HNSW (cosine similarity) \\
          Embeddings: & gemini-embedding-001 (3072-dim) \\
          Graph:      & entities + typed relations \\
          Retrieval:  & Hybrid RAG (vector + graph) \\
          Cache:      & Redis (1 h TTL) \\
        \end{tabular}
      \end{tcolorbox}

    \column{0.48\textwidth}
      \begin{tcolorbox}[greencard, title={\faProjectDiagram\; Multi-Agent Protocol}]
        \scriptsize\ttfamily
        \begin{tabular}{@{}ll@{}}
          Pattern:  & AgentClient ABC \\
          Routing:  & capability-based dispatch \\
          Persist:  & agent\_connectors table (DB) \\
          Register: & POST /agents/register \\
          VoiceOS:  & WS /v1/ws (JSON contract) \\
          VisualOS: & REST /analyze (base64 img) \\
        \end{tabular}
      \end{tcolorbox}
      \vspace{4pt}
      \begin{tcolorbox}[ambercard, title={\faServer\; API Surface}]
        \scriptsize\ttfamily
        \begin{tabular}{@{}ll@{}}
          Layer:          & FastAPI + Pydantic \\
          POST /chat      & JSON + base64 image \\
          GET  /ask       & plain text (iOS Shortcuts) \\
          WS   /v1/ws     & VoiceOS contract \\
          GET  /agents    & registry list \\
          SSE  /computer/stream & computer agent \\
        \end{tabular}
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ── 14. Influencers ────────────────────────────────────────────────────────
\begin{frame}{For creators and influencers — four shareable ideas}
  \begin{columns}[T]
    \column{0.48\textwidth}
      \begin{tcolorbox}[skycard, title={``Finally — an AI that knows your life''}]
        \small
        Not a chatbot you re-explain yourself to every session. CollectiveOS knows your habits, contacts, and projects. It grows smarter with every conversation.
      \end{tcolorbox}
      \vspace{5pt}
      \begin{tcolorbox}[indigocard, title={``Voice-first future — already here''}]
        \small
        Say ``hey collective'' and your entire digital life responds. Control your home, check health, send messages — all through natural conversation.
      \end{tcolorbox}

    \column{0.48\textwidth}
      \begin{tcolorbox}[greencard, title={``Your data stays yours. Always.''}]
        \small
        Every major AI assistant sends your data to a cloud. CollectiveOS runs on your machine, stores in your database, and never hands your personal history to anyone.
      \end{tcolorbox}
      \vspace{5pt}
      \begin{tcolorbox}[ambercard, title={``Not an app. An OS for your life.''}]
        \small
        ChatGPT forgets you after each session. CollectiveOS remembers your contacts, projects, and habits — building on that knowledge over time. It's infrastructure you live in.
      \end{tcolorbox}
  \end{columns}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{Under the Hood}
% ═══════════════════════════════════════════════════════════════════════════

% ── 15. Runtime ────────────────────────────────────────────────────────────
\begin{frame}{Runtime stack — every component earns its place}
  \centering
  \small
  \setlength{\tabcolsep}{7pt}
  \renewcommand{\arraystretch}{1.4}
  \rowcolors{2}{cardbg}{white}
  \begin{tabularx}{\textwidth}{>{\bfseries\color{skyblue}}p{2.4cm}
                               >{\bfseries\color{accentcol}}p{3.2cm}
                               X}
    \arrayrulecolor{accentcol!20}
    \toprule
    {\normalfont\color{dimcol}\texttt{Component}} &
    {\normalfont\color{dimcol}\texttt{Technology}} &
    {\normalfont\color{dimcol}\texttt{Why this choice}} \\
    \midrule
    LLM &
      Gemini Flash Lite &
      Fast, capable, cost-efficient. Router model handles intent. Swap anytime — the orchestration is model-agnostic. \\
    Agent Framework &
      LangGraph + PostgresSaver &
      Persistent checkpointing. Survives restarts. Native HITL via \texttt{interrupt\_before}. \\
    Memory &
      pgvector HNSW (3072-dim) &
      Semantic search over all history in the same DB as structured data. \\
    Voice &
      Gemini Live (native audio) &
      Full-duplex, real-time STT + TTS. Wake word. \texttt{gemini-2.5-flash-native-audio-latest}. \\
    Vision &
      VisualOS (Gemini Vision) &
      Screen → structured card. \texttt{scan\_session\_id} enables multi-turn follow-up. \\
    API Layer &
      FastAPI + Pydantic &
      REST + WebSocket + SSE. Bearer token auth. Port 8000. \\
    Cache &
      Redis (optional) &
      1 h TTL on session state. Falls back to in-process dict gracefully. \\
    Scheduler &
      APScheduler &
      Routines (cron), Condition Watchers (interval), Morning Briefings. \\
    \bottomrule
  \end{tabularx}
\end{frame}

% ═══════════════════════════════════════════════════════════════════════════
\section{Decision Brief}
% ═══════════════════════════════════════════════════════════════════════════

% ── 16. Decision Brief ─────────────────────────────────────────────────────
\begin{frame}{Seven questions — seven answers — everything you need to decide}
  \small
  \setlength{\tabcolsep}{8pt}
  \renewcommand{\arraystretch}{1.38}
  \rowcolors{2}{cardbg}{white}
  \begin{tabularx}{\textwidth}{>{\bfseries\color{skyblue}}p{2.8cm} X}
    \arrayrulecolor{accentcol!20}
    \toprule
    {\normalfont\color{dimcol}\texttt{Question}} &
    {\normalfont\color{dimcol}\texttt{Answer}} \\
    \midrule
    What is it? &
      Personal AI OS. One agent connecting every digital service — acting on your behalf. \\
    Who is it for? &
      Anyone using 5+ digital services daily who wants them unified, voice-first, and private. \\
    Working today? &
      85+ live tools: productivity, communication, smart home, health, finance, vision, voice, robotics. \\
    Where does data go? &
      Nowhere. Postgres is local. Only the current query reaches the model API — no personal history in the cloud. \\
    Is it safe? &
      Reads: instant. Writes and device control: \hl{always requires explicit confirmation}. Safety-first by architecture. \\
    Why defensible? &
      The orchestration architecture — not the model. Hub-and-spoke, persistent memory, growing connector graph. \\
    What's the vision? &
      The OS for your personal life. Infrastructure you live in. Smarter every day. \\
    \bottomrule
  \end{tabularx}
\end{frame}

% ── 17. Closing ────────────────────────────────────────────────────────────
{
\setbeamercolor{background canvas}{bg=accentcol}
\begin{frame}[plain]
  \centering
  \vspace{1.8em}

  % Quote
  {\fontsize{18}{22}\selectfont\bfseries\color{white}
    ``The best AI assistant isn't the one\\
    with the biggest model.}

  \vspace{0.4em}
  {\fontsize{18}{22}\selectfont\bfseries\color{skyblue}
    It's the one that knows \textit{you.}''}

  \vspace{2.0em}

  % Summary pill row
  \begin{tikzpicture}
    \foreach \lbl/\x in {
      {Built in phases}/0,
      {Proven at each step}/4.2,
      {Private by design}/8.4
    }{
      \fill[white!10] (\x-1.9,-0.28) rectangle (\x+1.9,0.28);
      \draw[white!25] (\x-1.9,-0.28) rectangle (\x+1.9,0.28);
      \node[font=\scriptsize\color{white!70}] at (\x,0) {\lbl};
    }
  \end{tikzpicture}

  \vspace{1.4em}
  {\small\color{white!35}\ttfamily
    github.com/Anurag9Dhiman/CollectiveOS}
\end{frame}
}

% ═══════════════════════════════════════════════════════════════════════════
\end{document}
