# Copy this file to .env and fill in your values.
# Never commit .env to git.

# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Postgres connection (defaults to docker-compose.yml credentials if not set)
DATABASE_URL=postgresql://assistant:assistant@localhost:5432/assistant

# Optional: single-user auth token for the FastAPI layer (Phase 5+)
# API_TOKEN=your-secret-token-here

# Telegram bot — two-way chat interface + proactive routine notifications
# 1. Message @BotFather → /newbot → copy the token below
# 2. Start a chat with your bot, send any message, then visit:
#    https://api.telegram.org/bot<TOKEN>/getUpdates  → copy your chat_id
# 3. Set TELEGRAM_WEBHOOK_SECRET to any random string (e.g. openssl rand -hex 16)
# 4. After deployment, register the webhook:
#    curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://<domain>/telegram/webhook/<SECRET>"
# TELEGRAM_BOT_TOKEN=123456789:ABCdef...
# TELEGRAM_CHAT_ID=123456789
# TELEGRAM_WEBHOOK_SECRET=your-random-secret
# Telegram bot — used for proactive routine notifications and message tools
# 1. Message @BotFather on Telegram → /newbot → copy the token below
# 2. Start a chat with your bot and send any message, then visit:
#    https://api.telegram.org/bot<TOKEN>/getUpdates  → copy your chat_id
# TELEGRAM_BOT_TOKEN=123456789:ABCdef...
# TELEGRAM_CHAT_ID=123456789

# Todoist personal API token
# Get it from: https://app.todoist.com/app/settings/integrations/developer
# TODOIST_API_TOKEN=your-todoist-token-here

# Home Assistant
# HASS_URL=http://homeassistant.local:8123
# HASS_TOKEN=your-long-lived-token  (Profile → Security → Long-Lived Access Tokens)

# Your local timezone for calendar events (IANA format).
# Default is UTC if not set.
# TIMEZONE=America/Toronto

# Spotify (for music playback control across all Spotify Connect devices)
# 1. Create a free app at https://developer.spotify.com/dashboard
# 2. Add redirect URI: http://localhost:8888/callback
# 3. Copy Client ID and Client Secret below
# SPOTIFY_CLIENT_ID=your-client-id
# SPOTIFY_CLIENT_SECRET=your-client-secret
# SPOTIFY_REDIRECT_URI=http://localhost:8888/callback

# Web search via Tavily (free tier: 1 000 searches/month)
# Sign up at https://tavily.com, then copy your API key below.
# TAVILY_API_KEY=tvly-...

# Telegram bot (for reading messages and sending replies)
# 1. Open Telegram and message @BotFather → /newbot → follow prompts
# 2. Copy the API token BotFather gives you
# 3. TELEGRAM_CHAT_ID is optional — your personal chat ID for "send to me" replies
#    Find it by messaging @userinfobot on Telegram
# TELEGRAM_BOT_TOKEN=123456789:ABCdef...
# TELEGRAM_CHAT_ID=987654321
# Notion integration
# 1. Go to https://www.notion.so/my-integrations → New integration
# 2. Copy the "Internal Integration Secret" below
# 3. Open each Notion page/database you want to use → "..." menu → Add connections → your integration
# NOTION_API_KEY=secret_...

# GitHub personal access token
# 1. Go to https://github.com/settings/tokens → Generate new token (classic)
# 2. Scopes: repo (for private repos) or public_repo (public repos only)
# 3. For fine-grained tokens: Contents + Issues + Pull requests (read) + Checks (read)
# GITHUB_TOKEN=ghp_...

# Slack bot integration
# 1. Go to https://api.slack.com/apps → Create New App → From scratch
# 2. OAuth & Permissions → Bot Token Scopes — add:
#    channels:read, groups:read, channels:history, groups:history,
#    im:history, mpim:history, chat:write, users:read, im:write
# 3. Install App to Workspace → copy "Bot User OAuth Token"
# 4. Invite the bot to channels: /invite @your-bot-name
# SLACK_BOT_TOKEN=xoxb-...

# Health data — Oura Ring (optional; if unset, uses iOS Shortcuts bridge instead)
# 1. Go to https://cloud.ouraring.com/personal-access-tokens
# 2. Create a token and copy it below
# iOS Shortcuts bridge (no Oura? build a Shortcut that POSTs to POST /health-ingest):
#   Body: {"date": "YYYY-MM-DD", "source": "apple_health",
#          "metrics": {"steps": 8000, "sleep_hours": 7.5, "hrv": 45,
#                      "resting_heart_rate": 58, "active_calories": 400}}
# OURA_TOKEN=...

# Car — Tesla (select brand in Settings ⚙ → Car, or set CAR_BRAND=tesla here)
# TESLA_TOKEN=your-tesla-fleet-api-token
# TESLA_VEHICLE_ID=       (optional; defaults to first vehicle on the account)
# TESLA_FLEET_BASE_URL=   (optional; defaults to NA region)
# BMW/MINI: CAR_BRAND=bmw  BMW_USERNAME=...  BMW_PASSWORD=...  BMW_VIN=...
# Ford/Lincoln: CAR_BRAND=ford  FORD_USERNAME=...  FORD_PASSWORD=...
# CAR_BRAND=tesla

# Smart Appliances — Samsung SmartThings (select brand in Settings ⚙ → Smart Appliances)
# 1. Go to https://account.smartthings.com/tokens → Generate new token
#    Scopes: List all devices, Control all devices
# 2. Copy the token below
# SMARTTHINGS_TOKEN=your-smartthings-token
# LG ThinQ: APPLIANCE_BRAND=lgthinq  LG_USERNAME=...  LG_PASSWORD=...  LG_COUNTRY=US
# APPLIANCE_BRAND=smartthings
# Finance — Plaid (read-only; bank balances and transactions)
# 1. Create a free account at https://dashboard.plaid.com/
# 2. Create an app → copy Client ID and Secret (Development or Sandbox)
# 3. Run the one-time Link flow to connect your bank:
#      PLAID_CLIENT_ID=... PLAID_SECRET=... PLAID_ENV=sandbox \
#      python src/connectors/finance_setup.py
#    Opens a browser page — connect your bank and the access token prints to terminal
# Sandbox test credentials: username user_good / password pass_good
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ACCESS_TOKEN=
# PLAID_ENV=development
